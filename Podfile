# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ShiftPickerView' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for ShiftPickerView

 pod ‘Masonry’

  target 'ShiftPickerViewTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ShiftPickerViewUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
